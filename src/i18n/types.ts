export type TranslationKeys = {
    streamAlert: {
        title: string;
        nowPlaying: string;
        duration: string;
        viewers: string;
        joinStream: string;
    };
};

export type Locale = 'en' | 'uk';